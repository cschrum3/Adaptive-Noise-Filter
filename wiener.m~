function [postNoise] = wiener(preNoise,preSignal)

%Sxy = abs(cpsd(preNoise,preSignal,[],[],(length(preNoise) * 2) - 1));
[Ss
[Sxx,~] = periodogram(preNoise',[],(length(Sxy) * 2) - 1);

figure
subplot(2,1,1)
plot(Sxy')
subplot(2,1,2)
plot(Sxx')


postNoise = Sxy/Sxx;
end